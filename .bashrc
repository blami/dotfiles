# ~/.bashrc - Bash configuration
# This file is sourced by Bash for both interactive and non-interactive shells.

# interactive shells only
[ -z $PS1 ] && exit

# disable history
unset HISTSIZE
unset HISTFILE
