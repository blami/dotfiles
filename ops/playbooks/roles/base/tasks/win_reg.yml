---
# Configure Windows registry keys

- name: Setting Windows HKLM registry keys
  win_regedit:
    path: "HKLM:\\{{ item.path }}"
    name: "{{ item.name }}"
    data: "{{ item.data }}"
    type: "{{ item.type }}"
  loop_control:
    label: "{{ item.path }}.{{ item.name }}={{ item.data}}"
  with_items: "{{ windows_registry }}"
