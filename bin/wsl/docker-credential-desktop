#!/bin/sh
# This is simple hack to lure Docker inside WSL connecting to Docker Desktop
# for Windows to use its credential storage.

/mnt/c/Program\ Files/Docker/Docker/resources/bin/docker-credential-desktop.exe $@
